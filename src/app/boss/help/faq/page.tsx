'use client';

// 도움말 → 자주 묻는 질문 — Industry 패턴
// boss/settings/faq 와 같은 화면을 재사용한다(privacy · terms 와 같은 방식).
// 상위 링크(← 도움말)는 헤더(PAGE_META)가 경로에서 파생한다.
export { default } from '@/app/boss/settings/faq/page';
